# 研究時間管理システム 設計書

## 1. 概要

### 1.1 目的

- 研究活動の作業時間をジャンル別・プロジェクト別に計測・記録
- **AIが週次・日次のスケジュールを自動生成**し、タスク選択の負担を排除
- 「やりたいこと」に偏らず、**締め切り・重要度に基づいた最適配分**を実現
- **Claude.aiとの会話ですべてを操作**（MCP連携）
- **2つのビュー**で状況把握：
  - **Claude.ai Artifact**: 会話中にサッと確認
  - **Reflexダッシュボード**: 詳細分析、週次レビュー（外出先からもアクセス可能）

### 1.2 基本方針

- **PostgreSQL中心の運用**: タスク・プロジェクトのマスターデータは自前DBで管理
- **自前タイマー実装**: 外部サービス依存なし、データ一元化
- **自前サーバーで自動化**: FastAPIでDB/Google Calendar/Claude APIを連携
- **Claude.aiで全般的な相談**: プロジェクト作成、スケジュール調整、タスク分解・統合すべて会話ベースで実施
- **Reflexダッシュボードで可視化**: カンバン、タイムラインを読み取り専用で表示
- **動的なタスク管理**: 作業を進めながらタスク粒度を柔軟に調整
- **Cloudflare Access必須**: 認証レイヤーによるセキュリティ確保

### 1.3 UIの役割分担

| 用途 | Claude.ai Artifact | Reflexダッシュボード |
|------|-------------------|---------------------|
| 今日の予定確認 | ◎ | ○ |
| カンバン（簡易） | ◎ 4〜8タスク | ◎ 全タスク |
| タスク操作 | ◎ 会話で指示 | × 読み取り専用 |
| 週次タイムライン | △ | ◎ Plotly詳細 |
| 統計・分析 | × | ◎ |
| 見積もり精度分析 | × | ◎ |

---

## 2. システム構成

### 2.1 全体アーキテクチャ

```
┌─────────────────────────────────────────────────────────────────┐
│                         ユーザー操作                            │
│  ┌─────────────────────────────┐    ┌─────────────────────────┐│
│  │   Claude.ai (Integrations)  │    │  Reflexダッシュボード   ││
│  │  ┌────────┬──────────────┐  │    │  （外部からアクセス可） ││
│  │  │チャット │  Artifact    │  │    │  - 詳細カンバン         ││
│  │  │        │ 簡易カンバン  │  │    │  - 週次タイムライン     ││
│  │  │        │ 今日の予定   │  │    │  - 統計・分析           ││
│  │  └────────┴──────────────┘  │    └────────────┬────────────┘│
│  │       【日常利用】          │      【詳細分析】│            │
│  └─────────────┬───────────────┘                 │            │
└────────────────┼─────────────────────────────────┼────────────┘
                 │                                 │
                 ▼                                 ▼
┌─────────────────────────────────────────────────────────────────┐
│              Cloudflare Access（認証レイヤー）【必須】           │
│  ├─ Claude.ai (MCP)  → Service Token 認証                      │
│  └─ ブラウザ（人間） → メール認証 or Google/GitHub OAuth        │
└─────────────────────────────────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────────────────┐
│                      Cloudflare Tunnel                          │
│  ├─ api.research-tracker.your-domain.com    → FastAPI (:8000)  │
│  └─ dashboard.research-tracker.your-domain.com → Reflex (:3000)│
└─────────────────────────────────────────────────────────────────┘
                 │ 暗号化トンネル（ポート開放不要）
                 ▼
┌─────────────────────────────────────────────────────────────────┐
│                 自宅サーバー (Docker Compose)                    │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │              FastAPI Server + MCP                         │  │
│  │  MCP Tools: get_kanban_view, create_task, reschedule...   │  │
│  │  REST API:  /dashboard/kanban, /dashboard/timeline...     │  │
│  └───────────────────────────────────────────────────────────┘  │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────────────┐   │
│  │PostgreSQL│ │  Reflex  │ │  pgweb   │ │   cloudflared    │   │
│  │  :5432   │ │  :3000   │ │  :8081   │ │    (tunnel)      │   │
│  └──────────┘ └──────────┘ └──────────┘ └──────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
```

### 2.2 使用ツール・サービス

| ツール | 役割 | 費用 |
|--------|------|------|
| **PostgreSQL** | マスターデータ管理 + 時間記録 | 無料 |
| **pgweb** | DB管理WebUI（LAN内のみ） | 無料 |
| **Reflex** | 詳細ダッシュボード（Python製） | 無料 |
| **Cloudflare Tunnel** | 安全な公開 | 無料 |
| **Cloudflare Access** | 認証レイヤー【必須】 | 無料（50ユーザーまで） |
| **Claude.ai Integrations** | MCP連携 | Pro以上 |
| Google Calendar | スケジュール表示 | 無料 |
| FastAPI | API + MCPサーバー | 自宅サーバー |
| Claude API | スケジューリング判断 | 従量課金 |

---

## 3. Claude.ai との連携

### 3.1 対話パターン一覧

| パターン | トリガー | 主な操作 | API |
|---------|---------|---------|-----|
| **0. プロジェクト作成** | 「〇〇を始めたい」 | create → breakdown | `/projects/create`, `/tasks/breakdown` |
| **1. 初期分解** | 新プロジェクト作成時 | breakdown → create-batch | `/tasks/breakdown`, `/tasks/create-batch` |
| **2. 細分化** | 「思ったより大変」 | split（1→多） | `/tasks/split` |
| **3. 統合** | 「細かすぎた」 | merge（多→1） | `/tasks/merge` |
| **4. 追加** | 「〇〇が必要になった」 | create + reschedule | `/tasks/create`, `/schedule/reschedule` |
| **5. 調整** | 「見積もりを修正したい」 | update | `PATCH /tasks/{id}` |
| **6. 振り返り** | 「今週どうだった？」 | summary + 次週計画 | `GET /tasks`, `/schedule/weekly` |

### 3.2 タイマー操作の会話例

| 発話 | 動作 |
|------|------|
| 「論文執筆始める」 | タスク特定 → `start_timer` → time_entriesにINSERT |
| 「終わり」「ストップ」 | `stop_timer` → end_time設定 → tasks.actual_hours更新 |
| 「今何やってる？」 | `get_timer_status` → 実行中タスク表示 |
| 「昨日の実験、2時間記録して」 | `add_time_entry` → 手動で時間記録 |

### 3.3 Artifact テンプレート

Claude.ai で表示する3種類のArtifact：

| Artifact | 内容 | 用途 |
|----------|------|------|
| **簡易カンバン** | Todo/Doing/Waiting/Done の4列、タスクカード | 全体把握 |
| **今日のスケジュール** | 時間帯別タスク、進行状況表示 | 1日の流れ確認 |
| **週次サマリー** | 日別バー、プロジェクト別集計 | 週次レビュー |

---

## 4. Reflex ダッシュボード

### 4.1 画面構成

```
┌─────────────────────────────────────────────────────────────────┐
│  Research Time Tracker                     [🔄 更新] [⚙️ 設定]  │
├─────────────────────────────────────────────────────────────────┤
│  [カンバン] [タイムライン] [週次] [統計] [分析]                  │
├─────────────────────────────────────────────────────────────────┤
│                     メインコンテンツ                            │
├─────────────────────────────────────────────────────────────────┤
│  今週 12.5h/30h | 今日 2.5h/6h | 締切間近: 3件                  │
└─────────────────────────────────────────────────────────────────┘
```

### 4.2 各ビューの機能

| ビュー | 機能 |
|--------|------|
| **カンバン** | 全タスク表示、フィルタ、ソート |
| **タイムライン** | Plotlyで日次の予定vs実績 |
| **週次** | プロジェクト/ジャンル別積み上げグラフ |
| **統計** | 見積もり精度、ジャンル別配分 |
| **分析** | 見積もり精度推移、コンテキストスイッチ分析 |

### 4.3 アクセス方法

| 場所 | URL |
|------|-----|
| **外部（インターネット）** | `https://dashboard.research-tracker.your-domain.com` |
| **LAN内** | `http://localhost:3000` |

※外部アクセス時はCloudflare Accessによる認証が必要

### 4.4 ディレクトリ構成

```
dashboard/
├── main.py           # Reflexアプリケーション
├── state.py          # 状態管理（FastAPIからデータ取得）
├── components/
│   ├── kanban.py     # カンバンコンポーネント
│   ├── timeline.py   # タイムラインコンポーネント
│   ├── weekly.py     # 週次ビューコンポーネント
│   ├── stats.py      # 統計コンポーネント
│   └── analysis.py   # 分析コンポーネント
└── rxconfig.py       # Reflex設定
```

---

## 5. Google Calendar 連携

### 5.1 カレンダー構成

| カレンダー | 用途 | 色 |
|-----------|------|-----|
| 研究タスク | AIが生成したスケジュール | 青 |
| 固定予定 | MTG、授業など | 赤 |

### 5.2 イベント形式

- **summary**: `[プロジェクト名] タスク名`
- **description**: タスクID、ジャンル、見積もり時間
- **start/end**: ISO 8601形式

---

## 6. Cloudflare Tunnel & Access 設定

### 6.1 認証の全体像

```
┌─────────────────────────────────────────────────────────────┐
│                    Cloudflare Access                        │
│                                                             │
│  アクセス元          認証方式              用途             │
│  ─────────────────────────────────────────────────────────  │
│  Claude.ai (MCP)  → Service Token      → API自動呼び出し   │
│  ブラウザ（人間） → メール認証/OAuth   → ダッシュボード閲覧│
└─────────────────────────────────────────────────────────────┘
```

### 6.2 Public Hostname 設定

| Hostname | 転送先 | 用途 |
|----------|--------|------|
| `api.research-tracker.your-domain.com` | `http://api:8000` | FastAPI（MCP含む） |
| `dashboard.research-tracker.your-domain.com` | `http://dashboard:3000` | Reflexダッシュボード |

### 6.3 Access Application 設定

**Application 1: API（MCP含む）**

| 項目 | 設定値 |
|------|--------|
| Name | Research Tracker API |
| Domain | `api.research-tracker.your-domain.com` |
| Policy | Allow: Service Token (Claude.ai用) |
| | Allow: Email ends with `@your-email.com` |

**Application 2: Dashboard**

| 項目 | 設定値 |
|------|--------|
| Name | Research Tracker Dashboard |
| Domain | `dashboard.research-tracker.your-domain.com` |
| Policy | Allow: Email ends with `@your-email.com` |
| | Allow: Google OAuth (your account) |

### 6.4 設定手順

#### Step 1: Cloudflare Tunnel 作成

1. Cloudflare Zero Trust → Networks → Tunnels → Create a tunnel
2. Tunnel名: `research-tracker`
3. トークンをコピー → `.env` の `CLOUDFLARE_TUNNEL_TOKEN` に設定

#### Step 2: Public Hostname 設定

1. 作成したTunnelを選択 → Public Hostname → Add
2. 以下を追加：
   - `api.research-tracker.your-domain.com` → `http://api:8000`
   - `dashboard.research-tracker.your-domain.com` → `http://dashboard:3000`

#### Step 3: Access Application 作成

1. Cloudflare Zero Trust → Access → Applications → Add an application
2. Self-hosted を選択
3. 上記6.3の設定を入力

#### Step 4: Service Token 作成（Claude.ai用）

1. Cloudflare Zero Trust → Access → Service Auth → Create Service Token
2. 名前: `claude-ai-mcp`
3. トークン情報を安全に保存

#### Step 5: Claude.ai Integrations 登録

1. Claude.ai → Settings → Integrations → Add Integration
2. 設定：
   - Name: `Research Time Tracker`
   - URL: `https://api.research-tracker.your-domain.com/mcp/sse`
   - Headers:
     - `CF-Access-Client-Id`: (Step 4で取得)
     - `CF-Access-Client-Secret`: (Step 4で取得)

---

## 7. ディレクトリ構成

```
research-scheduler/
├── api/
│   ├── app/
│   │   ├── main.py              # FastAPIアプリケーション
│   │   ├── config.py            # 設定・環境変数
│   │   ├── database.py          # SQLModel/PostgreSQL接続
│   │   ├── routers/
│   │   │   ├── mcp.py           # MCPエンドポイント
│   │   │   ├── projects.py      # プロジェクト操作API
│   │   │   ├── tasks.py         # タスク操作API
│   │   │   ├── schedule.py      # スケジューリングAPI
│   │   │   ├── dashboard.py     # ダッシュボード用API
│   │   │   └── sync.py          # 同期API
│   │   ├── mcp/
│   │   │   ├── server.py        # MCPサーバー
│   │   │   └── tools.py         # MCPツール定義
│   │   ├── services/
│   │   │   ├── db_service.py    # DB操作
│   │   │   ├── timer_service.py # タイマー操作
│   │   │   ├── gcal_service.py  # Google Calendar連携
│   │   │   ├── claude_service.py# Claude API連携
│   │   │   ├── scheduler.py     # スケジューリング
│   │   │   ├── task_service.py  # タスク管理
│   │   │   └── project_service.py# プロジェクト管理
│   │   ├── models/              # SQLModelモデル
│   │   └── middleware/
│   │       └── auth.py          # 認証
│   ├── Dockerfile
│   └── requirements.txt
├── dashboard/                    # Reflexダッシュボード
│   ├── main.py
│   ├── state.py
│   └── components/
├── docs/                         # 設計書
│   ├── 01-system-overview.md
│   ├── 02-database-design.md
│   ├── 03-api-design.md
│   └── 04-mcp-design.md
├── docker-compose.yml
├── .env
└── README.md
```

---

## 8. Docker Compose 構成

```yaml
services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: research_tracker
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"  # LAN内のみ
    restart: unless-stopped

  api:
    build: ./api
    environment:
      DATABASE_URL: postgresql://${DB_USER}:${DB_PASSWORD}@db:5432/research_tracker
      CLAUDE_API_KEY: ${CLAUDE_API_KEY}
    depends_on:
      - db
    restart: unless-stopped

  dashboard:
    build: ./dashboard
    environment:
      API_URL: http://api:8000
    depends_on:
      - api
    restart: unless-stopped

  pgweb:
    image: sosedoff/pgweb
    environment:
      DATABASE_URL: postgresql://${DB_USER}:${DB_PASSWORD}@db:5432/research_tracker
    ports:
      - "8081:8081"  # LAN内のみ
    depends_on:
      - db
    restart: unless-stopped

  tunnel:
    image: cloudflare/cloudflared:latest
    command: tunnel --no-autoupdate run
    environment:
      TUNNEL_TOKEN: ${CLOUDFLARE_TUNNEL_TOKEN}
    depends_on:
      - api
      - dashboard
    restart: unless-stopped

volumes:
  postgres_data:
```

| サービス | ポート | 説明 |
|---------|--------|------|
| db (postgres:16) | 5432 | PostgreSQL |
| pgweb | 8081 | DB管理WebUI（LAN内のみ） |
| api | 8000 | FastAPIサーバー |
| dashboard | 3000 | Reflexダッシュボード |
| tunnel | - | Cloudflare Tunnel |

---

## 9. 運用フロー

### 9.1 初期セットアップ

1. PostgreSQL + pgweb セットアップ
2. Google Calendar セットアップ（GCP、OAuth2認証）
3. FastAPI サーバー構築
4. MCP サーバー実装
5. Cloudflare Tunnel & Access 設定
6. Reflex ダッシュボード構築
7. Claude.ai Integrations 接続
8. 動作確認

### 9.2 日常運用

| 時間帯 | 操作 | UI |
|--------|------|-----|
| **朝** | 「今日の予定を見せて」 | Claude.ai |
| **作業開始** | 「論文執筆始める」 | Claude.ai |
| **作業終了** | 「終わり」「ストップ」 | Claude.ai |
| **タスク変更** | 「見積もり8時間に変更して」 | Claude.ai |
| **リスケ** | 「予定変更があったのでリスケして」 | Claude.ai |
| **外出先** | ダッシュボードで進捗確認 | Reflex（外部アクセス） |
| **週末** | 統計・分析を確認 | Reflex |

### 9.3 UI使い分け

| シーン | 使用UI |
|--------|--------|
| 今日何すればいい？ | Claude.ai |
| タスク完了にして | Claude.ai |
| 見積もり精度どうだった？ | Reflex |
| 先月のプロジェクト別時間は？ | Reflex |
| 全タスクをざっと見たい | Reflex |
| 外出先で進捗確認 | Reflex（スマホブラウザ） |
| DB直接確認 | pgweb（http://localhost:8081） |

---

## 10. 実装チェックリスト

### Phase 1: PostgreSQL + 基本API
- [ ] PostgreSQLセットアップ
- [ ] SQLModelモデル定義（time_entries含む）
- [ ] 基本的なCRUD API

### Phase 2: MCP サーバー実装
- [ ] MCPサーバークラス実装
- [ ] 参照系ツール（get_kanban_view等）
- [ ] 操作系ツール（create_task等）
- [ ] スケジュール系ツール（reschedule等）
- [ ] タイマー系ツール（start_timer, stop_timer等）
- [ ] MCPエンドポイント（/mcp/sse, /mcp/messages）

### Phase 3: Cloudflare Tunnel & Access 設定
- [ ] Tunnel作成・トークン取得
- [ ] Public Hostname設定（API + Dashboard）
- [ ] Access Application作成（API用）
- [ ] Access Application作成（Dashboard用）
- [ ] Service Token作成（Claude.ai用）
- [ ] docker-compose.yml更新

### Phase 4: Claude.ai 連携テスト
- [ ] Integrations接続（Service Token設定）
- [ ] 各ツール動作確認
- [ ] Artifact表示テスト

### Phase 5: Reflex ダッシュボード
- [ ] カンバン/タイムライン/週次/統計ビュー
- [ ] 分析ビュー
- [ ] 外部アクセステスト

### Phase 6: Google Calendar連携
- [ ] Google Calendar API連携

### Phase 7: 運用テスト
- [ ] 1週間の試用運用
- [ ] 外出先からのアクセステスト

---

## 11. セキュリティ考慮事項

### 11.1 認証・認可

| 対象 | 認証方式 | 管理場所 |
|------|---------|---------|
| MCP API | Service Token | Cloudflare Access |
| Dashboard | メール認証 / OAuth | Cloudflare Access |
| pgweb | なし（LAN内のみ） | ファイアウォール |

### 11.2 ネットワーク

- **外部公開**: Cloudflare Tunnel経由のみ（ポート開放不要）
- **LAN内のみ**: pgweb (:8081), PostgreSQL (:5432)
- **暗号化**: HTTPS（Cloudflare自動管理）

### 11.3 注意事項

- Service Tokenは安全に保管（漏洩時は即座に再生成）
- Access Policyは最小権限の原則で設定
- 定期的にAccess Logsを確認

---

## 12. 将来の拡張案

- **対話型Artifact**: ドラッグ&ドロップでタスク移動
- **Slack/Discord通知**: 毎朝のスケジュール通知、リマインダー
- **モバイル対応**: Reflexレスポンシブ、PWA化
- **学習機能**: 見積もり精度向上、作業パターン学習
- **テンプレート機能**: 論文執筆、実験プロジェクト等のテンプレート
- **ショートカット対応**: デスクトップからワンクリックでタイマー開始

---

## 更新履歴

| 日付 | バージョン | 内容 |
|------|------------|------|
| 2025-01-07 | v1 | 初版作成（Toggl + Notion + Zapier構成） |
| 2025-01-07 | v2 | 自動スケジューリング機能追加、Clockifyに変更 |
| 2025-01-07 | v3 | 対話的タスク分解・統合機能追加 |
| 2025-01-07 | v4 | 対話的プロジェクト作成機能追加 |
| 2025-01-07 | v5 | Notion→PostgreSQL移行、Reflexダッシュボード追加 |
| 2025-01-07 | v6 | MCP連携、Cloudflare Tunnel、2層UI設計 |
| 2025-01-07 | v6.1 | Clockify → 自前タイマー実装に変更（外部依存削減） |
| 2025-01-07 | v6.2 | Cloudflare Access必須化、ダッシュボード外部公開 |
